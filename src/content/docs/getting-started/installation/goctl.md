---
title: Install goctl
description: Install the official go-zero code generation tool.
sidebar:
  order: 3
---

# Install goctl

```bash
go install github.com/zeromicro/go-zero/tools/goctl@latest
goctl --version
```

If the command is not found, ensure your `GOBIN` directory is included in `PATH`.
