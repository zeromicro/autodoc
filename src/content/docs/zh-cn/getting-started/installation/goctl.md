---
title: 安装 goctl
description: 安装 go-zero 官方代码生成工具 goctl。
sidebar:
  order: 3
---

# 安装 goctl

```bash
go install github.com/zeromicro/go-zero/tools/goctl@latest
goctl --version
```

若安装后命令不可用，请检查 `GOBIN` 是否在 `PATH` 中。
