---
title: 설정 참조
description: go-zero의 설정 참조에 대해 설명합니다.
sidebar:
  order: 2

---


## 목차

- [API 서비스 설정](overview/) — HTTP 서비스 설정 (`rest.RestConf`)
- [RPC 서비스 설정](api-config/) — gRPC 서비스 설정 (`zrpc.RpcServerConf` / `zrpc.RpcClientConf`)
- [서비스 설정](rpc-config/) — Base 서비스 설정 공유 통해 모든 서비스 타입 (`service.ServiceConf`)
- [로그 설정](service-config/) — 구조화 로깅 settings (`logx.LogConf`)
- [설정 자동 검증](log/) — 검증 설정 에서 시작 via `Validator` interface
